# 演示录屏脚本 · 视觉运动 Agent 系统（深蹲教练）

> 总时长目标：**3.5–4.5 分钟**｜ 形式：屏幕录制 + 真人出镜（摄像头画面本身在屏幕内）+ 口播
> 录制工具：OBS / EV 录屏均可，1080p，录**全屏**（含浏览器 + 终端小窗）

---

## 0. 录制前检查单（不录进成片）

- [ ] 终端 `cd pose-case`，执行 `python app.py`（摄像头模式），确认终端打印 `* 视频源: 0`
- [ ] 浏览器开 **http://127.0.0.1:5000** ，**只开这一个标签页**，关闭无关标签页和书签栏（Ctrl+Shift+B）
- [ ] 浏览器缩放 100%，窗口最大化；关闭系统通知（勿扰模式）
- [ ] 摄像头拍到全身侧面：人距摄像头 2–3 米，侧对镜头，光线充足，穿对比色上衣
- [ ] 确认 `tutorial_cps.json` 已存在（左侧教程要点秒出，不用等 VLM 提取）
- [ ] 预演一次：做一个深蹲，确认 rep 计数 +1、约 4 秒后 AI 反馈卡更新、TTS 出声
- [ ] 清空 `runs/` 和 `audio/*.mp3`（让成片从"第 1 次"开始，同时清除会话记忆）→ 重启 app
- [ ] 桌面打开一个终端小窗备用（镜 7 展示 runs/ 目录用）

**应急预案（卡壳就剪）**
| 意外 | 应对 |
|---|---|
| 摄像头打不开 | 换 `python app.py --camera 1`；再不行用 `python app.py --video test_squat.mp4`，口播改"用预录视频演示完整链路" |
| VLM 超时/失败 | 系统会自动降级为规则反馈（卡片会标注），口播顺势说"这是降级容错设计"，不 NG |
| TTS 没声音 | 点反馈卡上的"🔊 播放语音"按钮手动播一次 |
| rep 没被检测到 | 退回站立姿势停 1 秒再做；检查画面是否显示 No person detected |

---

## 镜头 1 ｜ 开场 + 系统全景（0:00–0:25）

**操作**：画面停在浏览器全页（顶部标题 + 左右双栏都入镜），先不动鼠标。

**口播**：
> "这是我做的视觉运动 Agent 系统——一个深蹲教练。左边是教程面板，右边是摄像头实时画面。
> 它做的事是闭环的：看教程、实时练、拿纠错、再验证改没改对。"

**看点**：标题栏"视觉运动 Agent 教练系统"、左教程右摄像头的双栏布局。

---

## 镜头 2 ｜ 教程面板：VLM 提取的教学要点（0:25–0:55）

**操作**：鼠标移到左侧教程面板，依次划过教程图和 4 条教学要点（CPs），可短暂停顿在"判定：…"那行小字上。

**口播**：
> "左边这个教程不是写死的。启动的时候，系统把教程 GIF 抽帧拼图，连同英文教程原文一起发给通义千问 qwen-vl-max，
> 自动提取出三到五条中文教学要点，每条还带量化判定建议——比如'下蹲至大腿与地面平行'。
> 这一步对应 AgentCoach 论文里 Coaching Points 到可判定参数的映射。"

**看点**：教程解剖图、"下蹲深度 / 膝关节角度 / 躯干姿态 / 双脚站位"4 条要点及判定小字。

---

## 镜头 3 ｜ 故意浅蹲 → 检测 + VLM 反馈 + TTS（0:55–1:50）

**操作**：人入镜侧站，先自然站直 2 秒（让状态机确认 STAND），然后**故意做一次很浅的蹲**（膝盖微弯就起身）。
做完回到画面外或侧后方，等 3–5 秒，鼠标移到底部结果区。

**口播**（边做边说）：
> "现在我做一次**故意的浅蹲**。右侧画面能看到 MediaPipe 的 33 个关键点和骨架是实时叠加的，上面还有左右膝角。"
（做完，等反馈时）
> "一次 rep 结束，状态机触发评审：系统把站立顶帧和最低点底帧拼成图，连同四个指标一起发给 VLM——
> 膝角 144 度、深度只有 15%、前倾 34 度，规则引擎先判了'深度不足'。"
（反馈卡出来、TTS 响起时停下让语音播完）
> "VLM 的反馈出来了：'下蹲深度不足，大腿未达到与地面平行'，还有一条可执行的建议。语音是 edge-tts 自动播报的。"

**看点**：骨架叠加实时跟随；rep 计数 0→1；指标卡 4 行数值与 ✓/✗；反馈卡"VLM 评审完成"+ 三段文字 + 语音自动播放。

**检查点**：反馈卡必须出现真实 VLM 文字（不是"评审中"）。没出来就等多 3 秒，再不行剪掉等待段。

---

## 镜头 4 ｜ 按建议纠正 → 修正前后对比（1:50–2:50）

**操作**：再入镜，这次**蹲到位**（大腿接近与地面平行）。做完等反馈。

**口播**：
> "这次我按它的建议：髋部往后推、想象坐椅子，蹲到位。"
（等反馈，卡上显示 ↑ 较上次改善 时指给镜头）
> "看这里——'↑ 较上次改善'。注意这个判定不是大模型拍脑袋：系统先把两次动作的四个指标按容差本地对比——
> 膝角从 144 降到 100 度以内、深度从 15% 上到 60% 以上，超过容差才判改善，抖动不算数；
> VLM 拿到这个判定后结合画面解释原因。'改善'和'达标'也是两个独立的判定。
> 这就是任务书要求的纠错验证：修正前后的状态变化有指标、有判定。"

**看点**：反馈卡的 **↑ 较上次改善** 芯片（绿色）；指标卡数值明显好于上次；severity 芯片变为"标准/小问题"。

**检查点**：若这次仍判不足（真人深度不够），就再蹲一次更深的，取成功那次，剪掉中间。

---

## 镜头 5 ｜ 实时规则横幅（2:50–3:20）

**操作**：再入镜，下蹲时**故意大幅弯腰前倾**（上身快贴腿），保持 2 秒让横幅触发，然后起身。

**口播**：
> "除了每次的 VLM 深度评审，还有一层零延迟的规则引擎：下降过程中一旦躯干前倾超过 45 度——
> 看，画面底部横幅直接弹'躯干过度前倾，挺胸收腹'。这层不过大模型，毫秒级，负责快；VLM 负责准。"

**看点**：画面底部红色横幅 ⚡ 出现（下降过程中，不是做完才出）。

---

## 镜头 6 ｜ 原始记录：可审阅可追溯（3:20–4:00）

**操作**：鼠标滚到"练习历史"，点第 1 次的"**▸ 查看原始记录**"，展开后慢慢划过：拼图 → Prompt → VLM 原始 JSON。

**口播**：
> "每次 VLM 调用的证据都落了盘。这是发给模型的关键帧拼图原图——左边顶帧、右边底帧；
> 这是完整的 Prompt，指标、规则初判、教学要点、本地改善判定都在里面；这是模型返回的原始 JSON。
> 真实调用、真实图片、全程可审阅可追溯。"

**看点**：展开区三段齐全（图 / Prompt / JSON）。

---

## 镜头 7 ｜ 收尾：架构一句话 + 存档目录（4:00–4:30）

**操作**：切到终端小窗，`dir runs`（或资源管理器打开 runs/ 展示一排 rep_XXXX 文件夹），然后切回浏览器全页停 2 秒结束。

**口播**：
> "架构上就是五层：MediaPipe 感知、状态机检测 rep、四个可解释指标、规则引擎实时提示、VLM 事件驱动评审，
> 全部调用存档在 runs 目录。这套'规则负责快、VLM 负责准、证据可追溯'的结构，同样可以搬到实验室自动化监督上。
> 谢谢。"

**看点**：runs/ 下一排 rep_0001、rep_0002… 文件夹；最后定格系统全页。

---

## 录制后剪辑要点

1. 剪掉所有"等 VLM 返回"的空白等待（>4 秒的部分），用跳切衔接；
2. 镜头 3、4 的 TTS 语音**保留原声**，口播避开与语音重叠；
3. 给两处加文字标注（后期）：镜头 4 的"↑ 较上次改善"芯片、镜头 6 的拼图；
4. 成片头部加 3 秒标题卡："视觉运动 Agent 系统 · 深蹲教练 — 二面演示"；
5. 导出 1080p mp4，命名 `demo_final.mp4`，与 report.html / report.pdf 放一起。
